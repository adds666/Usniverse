---
# Replaces the historical bootstrap that overwrote migrated roots and credentials.
# Requires restored app databases. Readarr/Bazarr are intentionally excluded.
- import_playbook: media_proxy_routes.yml
- import_playbook: media_connections.yml
